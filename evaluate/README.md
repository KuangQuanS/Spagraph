# evaluate workspace

- `scripts/`: entry scripts, notebooks, and method helpers that are worth backing up in git.
- `data/`: dataset-specific outputs, plots, and intermediate files. Large raw artifacts stay out of git on purpose.
