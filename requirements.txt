# Python version: 3.10.16 (recommended for reproducibility)
# Note: Results may vary with Python 3.12+ due to differences in random number generation
torch>=1.9.0
torchvision>=0.10.0
matplotlib>=3.4.0
tqdm>=4.60.0
transformers>=4.10.0
Pillow>=8.2.0
scikit-misc>=0.5.0

# Core scientific packages - locked to server versions for reproducibility
numpy==2.0.1
scipy==1.15.3
scikit-learn==1.6.1
pandas>=1.4.0

# Single-cell analysis packages - locked to server versions
scanpy==1.11.5
anndata==0.11.4
zarr<3.0.0  # anndata 0.11.4 requires zarr < 3.0

# Clustering packages - locked to server versions
leidenalg==0.10.2
python-igraph==0.11.9
