# evaluate/data

This folder holds dataset outputs, figures, and historical evaluation materials after the cleanup on 2026-03-14.

Large raw artifacts are intentionally excluded from git here, so the repository only keeps the lightweight evaluation scripts and folder layout.
